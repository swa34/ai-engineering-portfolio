# Graduation Announcement Demonstration — Evaluation Plan

**Evaluation specification with implementation evidence.** The local mock implementation passes 45 unit/API/resource tests and 16 browser tests. Results below identify the exercised scope; manual acceptance remains open. See the [verification record](../../demos/graduation-announcement/VERIFICATION.md) for environment, commands, test sources, and limitations. See the [technical specification](TECHNICAL_SPEC.md), [security requirements](SECURITY_DESIGN.md), and [accessibility requirements](ACCESSIBILITY.md).

## Execution method and evidence

The evaluation specifies deterministic unit/contract tests for schemas, canonical composition, and validation; integration tests against temporary SQLite databases and the API; and browser tests for the full workflow. Inject mock response fixtures and a controllable clock to exercise timeouts and late responses without paid requests or long sleeps. Test the public API and observable persisted outcomes, not merely UI button visibility or mocks of approval logic.

Fixtures derive only from `complete-standard` and `minimal-brief` or other independently invented records. A normal mock response is not evidence of real-model quality. Every test run records commit, date, runtime/dependency versions, command, fixture/scenario, database migration version, provider mode, expected result, actual result, pass/fail, and evidence location. Browser/manual runs additionally record browser, viewport, OS, and assistive technology where used. Never record sensitive content, tokens, or real source data in evidence.

Use the [shared resource budget](SECURITY_DESIGN.md#http-and-resource-controls) for transport and polling limits in these cases. In the matrix, U = unit/contract, I = API/storage integration, B = browser automation, and M = manual. Expected 4xx failures are passing tests only if the response and persisted invariants match expectations. An unexecuted case is neither a pass nor a fail.

## Required cases

| ID | Input/action and level | Expected behavior and evidence to capture | Actual behavior | Pass/fail |
| --- | --- | --- | --- | --- |
| E01 | Complete standard record; U/I/B | Submit freezes facts; generation is explicit; mock returns canonical candidate; unchanged passing revision can be approved and prepared with fictional label. Inspect snapshot/revision/approval/export IDs. | Unit/API/browser primary workflow passes; frozen source and exact approved artifact checked. | Pass (exercised automated scope) |
| E02 | Missing degree; U/I/B | DraftSource accepts the explicitly nullable fields, including a null whole preferences object; partially null preferences, null arrays/institution, and omitted keys fail. A saved draft missing degree fails SourceInput submission with 422, remains Draft, and creates no snapshot or attempt. | Draft nullability and missing-degree submission rejection checked; browser error remains linked to degree. | Pass (exercised automated scope) |
| E03 | Missing/false consent or fictional acknowledgment; U/I/B | No submission/generation; false fictional acknowledgment blocks client draft save. Server-created copied DraftSource may contain content with flags false; first owner save requires acknowledgment true, submission requires consent true too. Verify no premature snapshot. | Independent unchecked acknowledgments, save/submit guards, and copied-draft flags checked. | Pass (exercised automated scope) |
| E04 | Instruction-like phrase in quote; U/I | Source save returns 422 INSTRUCTION_LIKE_CONTENT and persists no submitted payload; direct adapter fixture cannot invoke actions and yields a blocker if otherwise schema-valid. | Source instruction rejection and blocking provider-instruction finding checked. | Pass (exercised automated scope) |
| E05 | Altered major/program with valid response shape; U/I/B | FACT_MISMATCH and/or NARRATIVE_MISMATCH, NeedsReview, approval 422, no approval row. Compare all three prose fields and facts_used. | Altered program retained with blockers; browser approval disabled. | Pass (exercised automated scope) |
| E06 | Invented honor; U/I | Adding an honor to facts_used or narrative produces blocking findings even if provider concern lists are empty. | Invented honor produces a blocking finding in unit/API fixtures. | Pass (exercised automated scope) |
| E07 | Unsupported or paraphrased quotation; U/I | Exact source quote required in standard mode; mismatch blocks approval. | Unsupported quote produces a blocking finding in unit/API fixtures. | Pass (exercised automated scope) |
| E08 | Inappropriate tone/template; U/I/B | A known template ID inconsistent with source preference passes schema, is retained in NeedsReview with TONE_MISMATCH, and cannot be approved. Unknown template ID fails the attempt schema with no revision. Added slang/praise yields NARRATIVE_MISMATCH. | Known wrong tone blocks; unknown template fails schema; narrative additions block. | Pass (exercised automated scope) |
| E09 | Provider timeout; I/B | At deadline fail attempt, return same cycle to Submitted, retain old revision if any, announce safe failure, offer explicit retry. | Deadline failure and explicit successful browser retry checked. | Pass (exercised automated scope) |
| E10 | Malformed/oversized output; U/I | Invalid JSON, extra/missing keys, wrong types, field lengths over schema bounds, or responses above the shared wire-byte cap fail with no candidate; Submitted remains retryable. Oversized human request gets 413, schema-length failure within the byte cap gets 422 and no revision; neither creates a LENGTH_EXCEEDED revision finding. | Strict malformed/oversized output, schema bounds, request body cap and no retained candidate checked. | Pass (exercised automated scope) |
| E11 | Reviewer requests revision; I/B | Reason required; ChangesRequested cannot approve; explicit generation reuses snapshot and creates another revision before review. | API/browser request-changes and explicit next generation checked. | Pass (exercised automated scope) |
| E12 | Regeneration after approval and after preparation; I/B | Separate Submitted child with same snapshot, no revision/approval; original approval/export unchanged; new generation remains explicit. | Separate regeneration child preserves source/approval references and existing artifact. | Pass (exercised automated scope) |
| E13 | Unauthorized approval; I/B | Contributor direct API attempts receive 403; administrator attempts receive 404 because the object is inaccessible, with no state/audit mutation claiming approval; tampered role fields rejected. | Direct contributor approval returns 403; administrator returns 404; no approval/decision row. | Pass (exercised automated scope) |
| E14 | Sensitive-pattern text; U/I/B | Independently invented email/phone/credential sentinels block persistence in source, revisions, and reasons; provider response fails without candidate; safe errors/logs never echo matches. | Synthetic screening sentinels rejected without reflected or persisted sensitive output. | Pass (exercised automated scope) |
| E15 | Inaccessible link text; B/M | Audit identifies a deliberately injected “Click here” fixture; actual request/review/export links have meaningful accessible names and destinations. | Actual controls/links exercised by descriptive accessible names; deliberate bad-link fixture/manual audit not run. | Partial; manual check open |
| E16 | Narrative-only invention; U/I | Keep facts_used correct but add unsupported text separately to headline, body, and social; each variant blocks approval. | Independent headline/body/social inventions each produce narrative mismatch. | Pass (exercised automated scope) |
| E17 | Human edit and restore; U/I/B | Edited schema-valid factual error creates immutable human revision and fresh failing run; original persists; restoration creates another revision and fresh passing run before approval. | Immutable human edit and explicit restored revision checked in API/browser. | Pass (exercised automated scope) |
| E18 | Stale approval; I/B | Approve prior revision/version after an edit returns 409; no approval; UI retains unsaved text and offers refresh. | Stale revision/version rejected; browser preserves unsaved text and requires refresh. | Pass (exercised automated scope) |
| E19 | Concurrent edits/decisions; I | Two mutations with the same expected version yield one commit and one 409; approve-versus-edit cannot approve stale text; duplicate approval cannot create two rows. | Both approval/edit dispatch orders yield exactly one commit and one stale-version rejection. | Pass (exercised automated scope) |
| E20 | Late provider success; I | Deliver success after timeout/withdrawal or after newer attempt starts; no stale revision or status overwrite, content-free discard event only. | Deferred late success after timeout cannot replace newer revision; safe discard event checked. | Pass (exercised automated scope) |
| E21 | Terminal rejection; I/B | Rejected cannot edit/generate/approve; restart creates Draft with acknowledgments false; rejected history remains intact. | Terminal rejection and separate restart with fresh flags checked in API/browser. | Pass (exercised automated scope) |
| E22 | Source correction; I/B | Server-created DraftSource contains copied content and false acknowledgments; prefilled UI explains fresh acknowledgment, save requires it, and submission requires fresh consent. New submission creates a new normalized snapshot; prior snapshot/cycles/revisions/approval/artifact remain unchanged. | Copied correction source and false flags checked in API/browser; prior cycle remains available. | Pass (exercised automated scope) |
| E23 | Export revision binding; I/B | Download requires prepared approval, contains exact approved bytes and labels; newer cycles cannot substitute content; unapproved/cross-object reads fail. | Exact stored/downloaded text and approval/revision/source IDs checked; protected reads enforced. | Pass (exercised automated scope) |
| E24 | Approval/audit transaction failure; I | Inject failure on audit insert; approval, status, and receipt roll back together; retry yields exactly one approval and event. | Injected audit failure rolls back approval; safe error and retry checked. | Pass (exercised automated scope) |
| E25 | Consent withdrawal race; I/B | Withdrawal versus approval/prepare is serialized; after withdrawal protected operations and artifact reads return 403, running attempt fails, history remains labeled. | Withdrawal blocks preparation/replay/download and fails an in-flight related cycle; history stays visible. | Pass (exercised automated scope) |
| E26 | Duplicate delivery/idempotency; I | Within the issuing valid session, same key/payload replays one request/cycle/attempt/revision/decision/export outcome; changed payload gets 409. After session expiry, restart, replacement, logout, or reset, old-session retry gets 401 regardless of receipt retention (reset deletes receipts); new sessions do not inherit keys. Recheck consent on replay and inspect existing work after reselecting a role. | Same-key replay across 8 mutation types preserves domain/event/receipt counts. Client tests cover lost committed responses, intervening actions, independent 503 keys, session clearing, and bounded pending delivery without eviction. | Pass (exercised automated scope) |
| E27 | Ownership/object access; I | A second test-only contributor session cannot access another request's sources, revisions, attempts, or exports by ID (404); administrator content access denied. | Second test owner receives 404 for nested objects and sees only own request list. | Pass (exercised automated scope) |
| E28 | Session/origin/CSRF; I/B | Missing/expired session 401; forged role ineffective; hostile Origin/Host and absent/wrong CSRF denied; role replacement rotates identity and token. | Host/Origin/CSRF, role tampering, replacement, expiry, and stale-cookie recovery checked. | Pass (exercised automated scope) |
| E29 | HTML/script payload; I/B | Source/provider strings render as inert text; no script, event handler, automatic link, or network request; candidate deviations remain blocked. | HTML-like text remains inert in Chromium; no script execution or external request. | Pass (exercised automated scope) |
| E30 | Input size/type boundaries; U/I | Exact field/array/body limits, duplicate normalized items, control characters, unknown fields, and coercions tested; invalid input does not write source data. | Strict types/keys, bounds, normalized duplicates, control characters and request size checked. | Pass (exercised automated scope) |
| E31 | Retry/capacity limits; I | One active attempt per cycle, two globally, three starts/cycle/minute, shared session limits, and dataset admission limits enforced; ordinary 429/409 denials create no excess work, explicit later retry succeeds. Two visible attempt pollers plus 60 ordinary reads over a rolling minute fit the shared budget; excess tab traffic returns 429 and pauses automatic reads for Retry-After without changing attempts. Already accepted attempts finish with reserved revision capacity even at the event threshold. | Request/cycle/revision/event caps, reserved slots, retries, two active pollers and excess-read backoff checked. Browser network/503 read recovery completes without starting a second generation. | Pass (exercised automated scope) |
| E32 | Crash/restart recovery; I/B | Persist running attempt then restart process; startup records PROCESS_INTERRUPTED and returns cycle to Submitted; reload recovers status, no silent retry. | Restart turns persisted running attempt into PROCESS_INTERRUPTED and invalidates sessions. | Pass (exercised automated scope) |
| E33 | Storage contention/completion failure; I | Lock timeout gives safe 503 without partial commit; failed completion cannot report success; deadline sweep/startup settles abandoned attempt. | Real separate-connection write lock returns 503; no partial write; same-key retry succeeds once. | Pass (exercised automated scope) |
| E34 | Advisory lists and missing fields; U/I | Nonempty concern/missing lists block even when prose matches; editor_notes cannot clear findings, act as instructions, or enter exports. | Concern/missing lists block matching prose; notes cannot clear findings. | Pass (exercised automated scope) |
| E35 | Minimal brief and Unicode boundaries; U/I | Minimal brief includes required facts and deliberately omits optional facts without a false missing-information failure. Check both tones, composed strings, and exact code-point boundaries. Maximum-field multibyte CandidateOutput and its human revision wrapper fit the shared byte caps and parse round-trip under compact direct UTF-8 and Unicode-escaped serialization. Canonically failing fixtures are retained with findings; canonical passing fixtures remain approvable. | Both templates, optional brief omission, normalization, Unicode maxima and escaped wire round-trips checked. | Pass (exercised automated scope) |
| E36 | Validator version change; I | Approval reruns current validator and binds its passing run; a newly failing revision remains NeedsReview; earlier approvals retain original version without recertification. | Changed validator persists a fresh failing run and prevents approval of a former pass. | Pass (exercised automated scope) |
| E37 | Reset with running workers; I/B | Only administrator with confirmation can reset; old callbacks cannot write into new dataset; old sessions/IDs fail; empty schema works, failed reset cannot report completion. | Typed/admin reset clears dataset and sessions; old in-flight callback cannot repopulate it. | Pass (exercised automated scope) |
| E38 | Logging, Git and provider isolation; I/M | Inspect safe logs and tracked inventory; no bodies, secrets, database files, local agent files, or employer materials; default startup and generation make zero external requests. | Provider payload excludes session/consent/review metadata; runtime browser stays local; deliverable paths inspected. | Pass (exercised automated scope) |
| E39 | Keyboard workflow and dialogs; B/M | Complete primary/recovery flows without mouse, visible focus, correct dialog focus/return, no traps or automatic approval. | Automated keyboard primary flow, Cancel/Escape focus, dirty navigation and native unload cancellation, historical-revision focus, and deliberate approval pass; full manual recovery audit open. | Partial; manual check open |
| E40 | Form errors and screen-reader flow; B/M | Labels, linked error summary, preserved input, logical comparison order, and one announcement per generation transition verified with recorded assistive technology. | Linked errors, focus and preserved input pass automated checks; screen-reader evaluation unavailable. | Partial; manual check open |
| E41 | Zoom, contrast, reduced motion; B/M | Meet project contrast/target requirements, reflow at specified zoom, wrap long values, preserve meaning without animation or color-only cues. | 320px reflow, reduced-motion emulation and axe checks pass; actual zoom and control/focus contrast open. | Partial; manual check open |
| E42 | Preview/print/export accessibility; B/M | Fictional notice and identifiers survive print, no clipped content, draft visibly unapproved, text attachment has meaningful sections. | Exact text download and print-media notices/control hiding pass; actual print-preview pagination open. | Partial; manual check open |
| E43 | Audit completeness and limits; I | All successful domain mutations have related events, safe decision reasons are traceable, event threshold blocks ordinary unaudited writes; terminal/recovery/withdrawal events still commit at the threshold, late discard is recorded once per attempt, reset remains possible. | Domain/event/receipt counts, audit rollback, event-threshold completion/withdrawal/download admission and reset checked. | Pass (exercised automated scope) |
| E44 | Clean-start setup and schema compatibility; I/M | Fresh checkout following implemented README runs offline mock after dependencies are installed; fresh or supported existing database passes integrity checks, startup failures have actionable safe errors. | Clean npm install/build and real HTTP workflow pass with new SQLite schema and integrity checks. Version 1 reopens with data/settings preserved; five missing/malformed/unsupported metadata fixtures reject without changing database bytes. | Pass (exercised automated scope) |

## Completion criteria

Implementation acceptance requires all required automated cases to pass and all manual checks to have recorded outcomes, with no unresolved violation of source/approval integrity, authorization, privacy boundaries, or essential keyboard/screen-reader workflows. Do not substitute code coverage or a successful happy path for failure and concurrency tests. A failed or unavailable manual check remains an explicit open gate.

Before reporting results, inspect persisted snapshots/revisions and compare them before/after edits, regeneration, failures, rejection, and withdrawal. For concurrency tests, assert counts and foreign-key relationships, not only HTTP statuses. For artifacts, compare exact bytes/digests against the approved revision and template version. Capture synthetic screenshots only after screens work.

Acceptance also requires lint/type checks, automated tests, accessibility review, a secret/content scan, and clean-start verification with recorded commands and results. No latency, accuracy, accessibility conformance, business benefit, or live-model performance claim is established by this specification.

## Current evidence

Application build: **implemented**. Executed automated checks: **45 unit/API/resource tests and 16 browser tests passed**, with zero axe violations across 16 checkpoints. Live-provider evaluation: **not run**. Manual accessibility acceptance remains open. A row marked as passing its exercised automated scope is not a claim that every manual or unexecuted variant in its expectation has passed. Full environment and evidence: [verification record](../../demos/graduation-announcement/VERIFICATION.md).
